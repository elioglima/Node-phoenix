import React, { useState, useEffect } from "react";

import {
    BASE,
    Processo
} from "./styled";

export default (props) => {
    return (
        <BASE>
            <Processo />
        </BASE>
    );
};
