import { call, put } from 'redux-saga/effects'
import { history } from '../../redux'

export function* controleModulos({ payload }) {

}


export function* controleMenu({ payload }) {
  console.log('ControleMenu', payload)
}